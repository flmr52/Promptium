{
  "name": "promptium",
  "version": "2.2.0",
  "description": "Promptium — Boost your AI. Prompt library & C.R.A.F.T. builder.",
  "type": "module",
  "scripts": {
    "dev": "npx serve src",
    "build": "node scripts/build.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "preview": "node tools/serve-preview.js"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "vitest": "^3.1.0",
    "@vitest/coverage-v8": "^3.1.0",
    "happy-dom": "^17.0.0",
    "playwright": "^1.52.0",
    "@playwright/test": "^1.52.0"
  }
}
